services:
  codebridgeai:
    build: .
    container_name: codebridgeai
    ports:
      - 5000:5000
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
