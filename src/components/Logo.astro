---
import { SITE } from 'astrowind:config';
import logoImage from '~/assets/images/logo_ho.png';
---

<img
  src={logoImage.src}
  alt={SITE?.name || 'Logo'}
  class="self-center ml-2 rtl:ml-0 rtl:mr-2 h-8 md:h-10 w-auto"
/>
